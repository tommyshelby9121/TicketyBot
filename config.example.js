module.exports = {
    // Discord Config
    token: "",
    prefix: "!",
    presence_type: "",
    presence_message: "",

    // Support Role
    support_role: "",

    // Logging
    log_command_executes: true,
    ticket_log_channel: "",

    // Creating Ticket
    ticket_parent: "",

    // DMing User
    dm_user: true,

    // Closing Ticket
    confirm_ticket_close: true,
    create_transcripts: true,
};